use a PostgreSQL DB to replace a Windows/IIS/SQL Server stack:
step 1: pull the postgres image and run
$ podman pull docker.io/library/postgres:17 --tls-verify=false                 //have to use postgres 17 for now
$ podman inspect docker.io/library/postgres                                    //to get the ExposedPorts (here 5432)
$ podman run --% -d --name my-postgres -p 1234:5432 -v pgdata:/var/lib/postgresql/data -e POSTGRES_PASSWORD=password docker.io/library/postgres:17     
                                                                               //host port:container port, host port value can be 1024-65535
$ podman ps                                                                    //verify
note: 
copy ouput contents ctrl+shift+C
remove a container: $ podman rm -f <container-name>

step 2: get into the container
$ podman exec -it my-postgres psql -U postgres
postgres=#                                          //output                                    
postgres=# CREATE DATABASE my_new_db
CREATE DATABASE                                     //output 
postgres=# \c my_new_db                             //switch to the new DB
my_new_db=#                                         //output
my_new_db=# \c postgres                             //switch back
postgres=#                                          //output
postgres=# \q                                       //quit
$                                                   //output

step 3: generate scripts in Microsoft SQL Server Management Studio to create SQL server DB schema





step 4: migrate schema and data
step 5: update connection strings

convert data from SQL Server to Postgres: use pgloader

schema conversion:
use SCT to do it first, then manually revise 2333...
SQL Server	                        PostgreSQL	                              
INT IDENTITY	                        GENERATED ... AS IDENTITY / SERIAL		                        
BIT	                                BOOLEAN	                                                        
NVARCHAR(MAX)	                        TEXT	                                                        
DATETIME	                        TIMESTAMP	                        	                        
UNIQUEIDENTIFIER	                UUID	                                                        
[ColumnName]	                        columnname / "ColumnName"	        	                        
dbo	                                public	                                partially auto-converted                      
GO	                                ;	                                partially auto-converted                        
GETDATE()	                        CURRENT_TIMESTAMP / NOW()	        	                        
ISNULL()	                        COALESCE()	                        	                        
@@ROWCOUNT	                        GET DIAGNOSTICS ... ROW_COUNT	        not auto-converted	                        

